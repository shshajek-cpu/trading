# UI 가이드 — 글래스모피즘 개편

이전 UX 감사에서 지적된 문제를 모두 반영하면서, 화면 배치와 겉모습을 유리(글래스모피즘)
기준으로 다시 짰다. 앞으로 화면을 손볼 때 이 문서의 규칙을 따른다.

---

## 1. 화면 배치

### 데스크톱

```
┌──────────────────────────────────────────────┬──────┐
│ 맨 위 바: 이름 · 화면분할 · 미니창           │      │
├───┬──────────────────────────────┬───────────┤ 오른 │
│그 │                              │           │ 쪽   │
│리 │    차트 (떠 있는 유리 판)     │  서랍     │ 아이 │
│기 │                              │ (열릴 때)  │ 콘   │
│레 │                              │           │ 레일 │
│일 │                              │           │      │
└───┴──────────────────────────────┴───────────┴──────┘
```

- **오른쪽 아이콘 레일**이 8개 설정 묶음의 유일한 입구다. 항상 보이고, 지금 열린 것에 불이 들어온다.
- **서랍은 레일을 덮지 않는다**(`right: 78px`). 덮으면 다른 묶음으로 건너뛸 수 없다.
- 레일의 `z-index`(47)가 서랍(45)보다 높다. 순서를 바꾸면 전환이 막힌다.
- 왼쪽 레일에는 그리기 도구만 둔다.

### 모바일 (≤900px)

**폰은 데스크톱을 좁게 접은 화면이 아니라 다른 앱이다.**
데스크톱 레이아웃을 반응형으로 줄이지 않고, 폰 전용 구조를 따로 그린다
(`App.tsx` 가 `isMobile` 에서 갈라져 `MobileApp` 을 그린다).

```
┌────────────────────────┐
│ BTC USDT   65,120  +2% │  종목 + 현재가 (앱의 얼굴)
│ (1m) 5m 15m 1h 4h 1d   │  주기 — 가로로 넘겨 고름
│ ┌────────────────────┐ │
│ │                    │ │
│ │       차트          │ │  남은 공간 전부
│ │              ○ ◉   │ │  떠 있는 도구(엄지 자리)
│ └────────────────────┘ │
│  차트   시세   알림  더보기 │  ← 탭바 (고정 4개)
└────────────────────────┘
```

**한 화면에 한 가지 일.** 탭 네 개로 나눈다.

| 탭 | 하는 일 |
|---|---|
| 차트 | 보는 화면. 도구는 떠 있는 버튼으로 최소화 |
| 시세 | 종목 검색·즐겨찾기. 누르면 바로 차트로 |
| 알림 | 가격 알림과 선 통과 알림을 한곳에 |
| 더보기 | 지표·핀·탐색·동기화 — 설정 앱처럼 줄로 |

규칙 몇 가지.

- 탭은 **네 개 고정**이고 위치가 변하지 않는다. 늘리고 싶으면 더보기 안으로 넣는다.
- 상세는 탭 **위로 밀려 들어오고**(`SubPage`) 뒤로가기로 닫힌다. 시트를 쌓지 않는다.
- 차트는 탭을 옮겨도 **살려 둔다**(`hidden` 으로만 감춤). 매번 다시 받으면 느리고 깜빡인다.
- 종목·시세는 **앱 헤더**가 크게 보여주므로 차트 칸 안의 머리띠는 접는다(`bare` 옵션).

데스크톱은 기존 구조(오른쪽 아이콘 레일 + 서랍)를 그대로 쓴다. 두 화면은 상태만 공유한다.

---

## 2. 유리(글래스모피즘) 규칙

유리는 **뒤에 뭔가 비쳐야** 유리로 보인다. 그래서 바탕에 청록·파랑·보라·붉은 광원 네 개를
깔아 블러가 물어갈 색을 만들어 뒀다(`index.css` 의 `body` 배경).

### 층위를 지킨다

같은 밝기의 유리를 여러 겹 쌓으면 깊이가 사라진다. 앞으로 나올수록 밝고 진하게 간다.

| 층위 | 토큰 | 쓰는 곳 |
|---|---|---|
| 바닥 | `--glass-1` (3.5%) | 패널 속 묶음 상자, 목록 배경 |
| 보통 | `--glass-2` (6%) | 기본 버튼·입력칸 |
| 위 | `--glass-3` (9%) | 선택된 항목, hover |
| 맨 위 | `--glass-4` (13%) | 켜진 버튼(active) |
| 판 | `--pane` / `--pane-solid` | 서랍·시트·드롭다운 |

블러도 같이 단계로 나눈다: `--blur-sm`(작은 조각) → `--blur`(바·카드) → `--blur-lg`(큰 판).
블러는 비싼 효과라 큰 판에만 강하게 건다.

### 두께를 만든다

유리는 **위쪽 하이라이트 + 아래쪽 그림자**가 있어야 두께가 생긴다.
`box-shadow: var(--sheen), var(--shadow)` 를 세트로 쓴다. 하나만 쓰면 종이처럼 납작해진다.

---

## 3. 손가락 규칙 (모바일)

- **모든 표적은 최소 44px**(`--tap`). 예외 없다.
- 보이는 크기는 작아도 된다. **닿는 범위만** 넓힌다.
  예: 색 고르는 칩은 30px 로 보이지만 `margin: 7px` 로 44px 를 확보한다.
- 입력칸 글자는 **16px 이상**. 그보다 작으면 아이폰이 화면을 확대한다.
- `hover` 효과는 `@media (hover: hover)` 안에만 쓴다. 폰에서는 눌린 상태가 눌어붙는다.

---

## 4. 앱처럼 동작하게 (모바일)

겉모습만 앱 같고 동작이 웹페이지면 금방 티가 난다. 아래는 '앱이 아니다'라고 느끼게 만드는
대표적인 것들이고, 모두 막아 뒀다.

| 웹페이지 티 | 어떻게 막았나 |
|---|---|
| 누르면 파란 네모가 번쩍 | `-webkit-tap-highlight-color: transparent` |
| 길게 누르면 글자가 선택됨 | `user-select: none` (값·입력칸만 예외로 되살림) |
| 길게 누르면 '복사/공유' 말풍선 | `-webkit-touch-callout: none` |
| 두 번 누르면 화면이 확대 | `touch-action: manipulation` |
| 화면 끝에서 튕김 | `overscroll-behavior: none` |
| 뒤로가기 누르면 앱이 꺼짐 | `useBackClose` — 열린 시트부터 닫는다 |

### 뒤로가기 (가장 중요)

앱에서는 무언가 열려 있을 때 뒤로가기를 누르면 **그것만 닫힌다.** 이 처리가 없으면
시트가 열린 채 뒤로가기를 눌렀을 때 앱이 통째로 꺼져 보던 화면을 잃는다.

`hooks/useBackClose.ts` 가 처리한다. 열릴 때 가짜 방문 기록을 하나 쌓고 뒤로가기가 그것을
소비하게 둔다. X 버튼으로 닫을 때는 쌓아둔 기록을 직접 걷어내므로 **기록이 쌓이지 않는다**
(열고 닫기를 몇 번 해도 뒤로가기 한 번이면 앱을 벗어난다).

새로 뜨는 겹침 요소를 만들면 반드시 이 훅을 붙인다.

### 안전 영역 (노치·홈바)

홈 화면에 추가해 전체화면으로 열면 상단 노치와 하단 홈바가 화면을 가린다.
`index.css` 에 `--sat`/`--sab`/`--sal`/`--sar` 로 모아 뒀으니 화면 가장자리에 붙는 것은
반드시 이 값을 더해서 배치한다.

- 상단바: `padding-top: var(--sat)`
- 아래 독·시트·알림 카드: `calc(... + var(--sab))`

### 설치 안내

브라우저 탭으로 열면 주소창이 화면을 깎아먹고, 아이폰은 알림도 아예 못 받는다.
`useInstallPrompt` 가 폰에서 한 번만 안내한다. 안드로이드는 버튼 한 번으로 설치되고,
아이폰은 설치 창을 열 수 없어 '공유 → 홈 화면에 추가'를 글로 안내한다. 닫으면 다시 안 뜬다.

---

## 5. 글자와 상태 표시

- **라벨은 상태에 따라 바뀌지 않는다.** 아래 독의 '수평선'은 켜져도 꺼져도 '수평선'이다.
  상태는 색과 테두리로 알린다. 글자가 바뀌면 누르기 전에 무엇인지 알 수 없다.
- 이모지 대신 **선 아이콘**(`Icon.tsx`)을 쓴다. 이모지는 기기마다 모양·크기가 달라 줄이 흐트러진다.
- 켜고 끄는 것은 네모 체크박스 대신 **스위치**(`input.switch`)를 쓴다.
- 같은 개념은 같은 말로 부른다. 패널 이름은 `lib/panels.ts` 한 곳에서만 관리한다.

---

## 6. 차트를 가리지 않는다

차트 위에 얹는 것은 최소한으로, 반드시 데이터가 적은 자리에 붙인다.

- **OHLC 정보바**: 왼쪽 위. 폰에서는 한 줄을 넘지 않게 거래량을 감춘다(`flex-wrap: nowrap`).
- **지표 이름표(RSI·MACD)**: 각 패널이 실제로 시작하는 y 좌표에 붙인다.
  고정 비율(`bottom: 34%`)로 얹으면 패널 높이를 바꿀 때마다 어긋난다.
  `Chart.tsx` 의 `onPaneLayout` 이 `paneSize()` 로 실제 높이를 재서 알려 준다.
- **현재가 배지**: 가격축 위. 가격 + 봉 마감까지 남은 시간을 한 덩어리로.

---

## 7. 이전 감사에서 고친 것

| # | 지적 | 어떻게 고쳤나 |
|---|---|---|
| 1 | 모바일에서 '지표'를 눌러도 지표가 화면 밖 | 시트가 묶음 하나만 담는다. 6개 탭을 쌓지 않는다 |
| 2 | 터치 표적이 44px 미만 | 모든 표적에 `--tap` 적용. 작은 칩은 여백으로 확보 |
| 3 | 아래 버튼 라벨이 상태에 따라 바뀜 | 라벨 고정, 상태는 색으로만 |
| 4 | 지표 패널 버튼이 데이터를 가림 | 실제 패널 좌표를 재서 왼쪽 위 여백에 배치 |
| 5 | 팝오버가 화면 경계에서 잘림 | 팝오버를 없애고 오른쪽 서랍으로 교체 |
| 6 | 상단 가격이 레이아웃 전환 중 겹침 | 머리띠에 그라데이션 판을 깔아 분리 |
| 8 | '시간대' ↔ '주기' 용어 불일치 | `lib/panels.ts` 한 곳에서 '주기'로 통일 |

---

## 8. 파일 지도

```
src/
  index.css              디자인 토큰(색·유리·그림자·모서리·모션) + 기본 요소
  App.css                화면 배치와 부품 스타일 (섹션 주석으로 구분)
  components/
    Icon.tsx             선 아이콘 묶음 — 이모지 대신 이걸 쓴다
    Toolbar.tsx          맨 위 바 (이름·분할·미니창)
    PanelHost.tsx        설정 그릇 — 데스크톱 서랍 / 모바일 시트
    PushBox.tsx          '앱 꺼도 알림' 안내 — 폰·데스크톱이 함께 쓴다
    mobile/              폰 전용 화면 (데스크톱과 공유하지 않는다)
      MobileApp.tsx      폰 앱 본체 — 탭 전환과 상세 페이지 관리
      MobileShell.tsx    뼈대 + 아래 탭바
      ChartScreen.tsx    차트 탭 (헤더·주기칩·떠 있는 도구)
      MarketsScreen.tsx  시세 탭 (검색·즐겨찾기)
      AlertsScreen.tsx   알림 탭 (가격 알림 + 선 알림)
      MoreScreen.tsx     더보기 탭 (설정 앱식 목록)
      SubPage.tsx        탭 위로 밀려 들어오는 상세 페이지
      AlertForm.tsx      알림 추가 (현재가 대비 % 빠른 선택)
  hooks/
    useBackClose.ts      뒤로가기로 열린 것 닫기 — 겹침 요소를 만들면 꼭 붙인다
    useInstallPrompt.ts  앱 설치 가능 여부·안내 상태
  lib/
    panels.ts            데스크톱 서랍 목록
    mobileNav.ts         폰 탭·상세 페이지 목록 — 이름·아이콘의 유일한 출처
```
