# UX 감사 진단서 — trading 웹앱

- 대상 빌드: `https://trading-136.pages.dev/` (앞 단계 촬영과 동일 배포본)
- 근거: `C:\tmp\audit\` 의 스크린샷 18장 전부 Read로 열어 눈으로 확인 + iPhone 13(390×844 CSS px) 라이브 뷰에서 `getBoundingClientRect` 실측
- 터치 최소 기준: 44×44 CSS px

---

## 심각도별 목록

### 치명

**1 / 모바일 '지표'를 눌러도 지표가 화면에 없다**
- 심각도: 치명
- 근거: `C:\tmp\audit\mob-sheet-indicator-top.png`(맨 위), `mob-sheet-indicator-mid.png`(중간), `mob-sheet-indicator-bottom.png`(맨 아래)
- 문제: 하단바 '지표'를 누르면 열리는 한 개의 긴 시트가 관심종목 → 매매핀 → (수평선 그리기) → 이동평균/RSI/MACD/거래량급증 → 알림 → 기기간 동기화 순으로 전부 세로로 쌓여 있다. 시트가 맨 위(관심종목·종목추가·매매핀 롱/숏/관망)에서 열려, 정작 이동평균/RSI/MACD는 실측상 y≈1000px 이하(844px 뷰포트 밖)에 있어 여러 번 스크롤해야 나온다. '지표'를 눌렀는데 지표가 안 보이므로 사용자가 목표를 달성하지 못한다.
- 고치기: 하단바 버튼별로 시트 내용을 분리(지표 버튼은 지표 섹션만)하거나, 최소한 '지표' 진입 시 지표 섹션 위치로 스크롤/탭 전환해서 첫 화면에 지표가 보이게 한다.

### 큰불편

**2 / 모바일 터치 대상 다수가 44px 미만**
- 심각도: 큰불편
- 근거: `mob-01-first.png`, `mob-sheet-indicator-top.png`, `mob-sheet-line.png` + 실측치
- 문제(실측, 폭×높이 CSS px): 분할 전환 ▢◫⊞ 34×28 · 종목 트리거 90×27 · 주기(1m) 트리거 90×27 · 관심종목 행 298×31 · 관심종목 삭제 × 24×28 · 기록/내지표 179×31 · 핀찍기시작 362×37 · 롱/숏/관망 118×33 · 지표 pane 펼침 ⌄ 30×30 · 톱니 ⛭ 30×30 · 수평선 색상칩 20×20 · 그린선 삭제 ✕ 20×12 · 가격/색 추가 46×31. 전부 높이가 44px에 못 미치고, 삭제 ×(24×28)·색상칩(20×20)·선삭제 ✕(20×12)는 엄지로 정확히 누르기 어렵다. (하단바 수평선/지표 149×44, ↩ 56×44 는 기준 충족)
- 고치기: 탭 가능한 행·버튼·칩의 높이를 최소 44px로, 삭제 ×/색상칩은 히트영역(padding)만이라도 44px로 키운다.

**3 / 모바일 하단 '수평선' 버튼이 눌리면 '✓ 차트 탭'으로 라벨이 바뀐다**
- 심각도: 큰불편
- 근거: `mob-01-first.png`(하단 '— 수평선') vs `mob-sheet-line.png`(같은 자리 '✓ 차트 탭')
- 문제: 같은 위치의 같은 버튼이 상태에 따라 '수평선' ↔ '✓ 차트 탭'으로 뜻이 바뀐다. 지금 무슨 모드인지, 이 버튼을 누르면 무엇이 되는지 예측이 안 된다.
- 고치기: 라벨을 상태와 무관하게 고정(예: '수평선')하고 활성/그리기중은 색·체크로만 표시한다.

**4 / 모바일 지표 pane의 펼침·톱니 버튼이 RSI/MACD 선 위에 겹쳐 데이터를 가린다**
- 심각도: 큰불편
- 근거: `mob-01-first.png` (RSI·MACD 창 좌하단 ⌄/⛭ 아이콘이 지표 라인 위)
- 문제: 30×30 플로팅 버튼 2개가 각 보조지표 창 좌하단에 얹혀 있어 그 구간의 라인·값이 가려진다. 값이 작을 때(바닥권)의 지표를 못 본다.
- 고치기: 버튼을 pane 밖(헤더 라인)으로 빼거나 반투명·자동 숨김 처리해 데이터 위를 덮지 않게 한다.

### 사소

**5 / 데스크톱 지표 팝오버 하단 '거래량 급증' 영역이 경계에서 잘린다**
- 심각도: 사소
- 근거: `desk-popover-3-indicators.png`
- 문제: 폭발 5 입력 아래가 팝오버 경계에서 끊겨 있고 스크롤 단서가 약해, 아래에 더 있는지 알기 어렵다.
- 고치기: 내부 스크롤 여백·스크롤바를 명확히 하거나 팝오버 높이를 콘텐츠에 맞춘다.

**6 / 모바일 첫 화면 우측 상단 가격축 라벨 잘림**
- 심각도: 사소
- 근거: `mob-01-first.png` 우상단 '65000.00'이 레이아웃 전환줄에 가려 절반 잘림
- 문제: 최상단 축 라벨이 잘려 읽기 애매하다.
- 고치기: 차트 상단 패딩을 늘려 축 라벨이 헤더에 겹치지 않게 한다.

**7 / 모바일 MACD 값 박스(-8.08) 하단이 x축 경계에서 살짝 잘린다**
- 심각도: 사소
- 근거: `mob-01-first.png`, `mob-sheet-line.png` (MACD 세 줄 값 박스 아래 '0' 조각이 삐져나옴)
- 문제: 값 배지가 창 경계를 넘어 하단이 잘린다.
- 고치기: 값 배지를 pane 안쪽 여백으로 당겨 배치한다.

**8 / 용어 불일치: '시간대' ↔ '주기'**
- 심각도: 사소
- 근거: `desk-popover-2-timeframe.png`(탭 '시간대', 안내문 '주기만 다르게')
- 문제: 같은 개념을 탭에선 '시간대', 설명문에선 '주기'로 달리 부른다.
- 고치기: 한 단어로 통일한다.

**9 / [규칙 대비 확인] 거래량 급증 마커가 초록/빨강 외 색을 쓴다**
- 심각도: 사소(규칙 명시 항목)
- 근거: `desk-popover-3-indicators.png`(범례 보통2배 노랑·강함3배 주황·폭발5배 자홍), `desk-01-first.png`·`mob-01-first.png`(거래량 바 노랑/주황/자홍)
- 문제: 규칙4는 시세 색상만 초록 #26a69a / 빨강 #ef5350을 허용하는데, 거래량 급증은 노랑·주황·자홍(보라) 3단계를 쓴다. 범례가 붙은 기능적 3단 스케일이라 사용성 문제는 아니나, 규칙과는 어긋난다 — 보고 규율에 따라 그대로 남긴다. (판단은 사람 몫)
- 고치기: 규칙을 예외로 명문화하거나, 급증 강도를 초록/빨강 명도 단계로 재설계한다.

---

## 먼저 고칠 5개 (순서대로)

1. **모바일 '지표'에 지표를 먼저 보이게** (항목1, 치명) — 하단바가 여는 시트를 섹션별로 분리/스크롤.
2. **모바일 터치 대상 44px 확보** (항목2, 큰불편) — 특히 삭제 ×·색상칩·선삭제 ✕.
3. **하단 '수평선/차트 탭' 라벨 토글 제거** (항목3, 큰불편) — 라벨 고정, 상태는 색으로.
4. **지표 pane 플로팅 버튼이 데이터 가리지 않게** (항목4, 큰불편) — pane 밖 이동 또는 자동 숨김.
5. **데스크톱 지표 팝오버 하단 잘림 해소** (항목5, 사소) — 스크롤 단서/높이 조정.

치명 1건, 큰불편 3건이 있으며, 없는 문제를 만들어 넣지 않았다.
